{
  "version": 2,
  "buildCommand": "npm run vercel-build",
  "installCommand": "npm install && mkdir -p prisma && curl -L https://github.com/EpaphrasSam/verse-catch/releases/download/db/bible.db -o prisma/bible.db",
  "outputDirectory": ".next"
}
